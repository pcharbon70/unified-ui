defmodule UnifiedUi.Dsl.State do
  @moduledoc """
  Struct representing the state definition in the DSL.

  This struct is created when users define initial state in their UI components.
  """

  defstruct [:attrs]
end
